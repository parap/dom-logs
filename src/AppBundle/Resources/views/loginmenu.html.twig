<fieldset class="login-menu">
<a href="{{ path('homepage')}}">Homepage</a>
{% if app.user %}
<a href="{{ path('fos_user_profile_edit')}}">Profile</a>
<a href="{{ path('fos_user_security_logout')}}">Logout</a>
{% else %}
<a href="{{ path('fos_user_security_login')}}">Login</a>
<a href="{{ path('fos_user_registration_register')}}">Register</a>
{#TODO: fix amazon email sending and uncomment the link #}
{#<a href="{{ path('fos_user_resetting_request')}}">Reset password</a>#}
{% endif %}
<a href="{{ path('contacts')}}">Contacts</a>
</fieldset>
